/// 빌드 시점에 굳는 플래그 (재도입 2026-09-18).
///
/// 런타임 설정이 아니다 — 유저가 켤 수 있는 스위치가 아니라 **어떤
/// 바이너리인가**를 가른다. `--dart-define`이 아니라 소스 상수인 이유는
/// 심사 빌드를 Xcode에서 직접 뜨기 때문이다 (dart-define은 `flutter
/// build/run`이 갱신하는 Generated.xcconfig에만 실려 Xcode 직접 빌드에서
/// 조용히 빠진다).
library;

/// App Store 심사 제출용 빌드인가.
///
/// 켜지면 온보딩 준비 확인 페이지의 앱스토어 별점 팝업을 건너뛴다
/// (§8.5 — 온보딩 중 평점 요구로 리젝, 2026-09-02. 2026-09-11에 플래그를
/// 지웠다가 2026-09-18 심사 재제출을 위해 되살렸다).
///
/// **평소에는 `false`** — 유저에게 팝업이 뜬다. 심사 빌드를 뜰 때만
/// `true`로 바꿔 굽고, 제출이 끝나면 곧바로 `false`로 되돌린다
/// (2026-09-19 롤백 완료).
///
/// ⚠️ 심사에 통과한 바이너리가 곧 스토어에 나가는 바이너리다 — `true`인
/// 채로 구운 빌드를 그대로 출시하면 유저에게도 팝업이 안 뜬다.
const bool kReviewBuild = false;
