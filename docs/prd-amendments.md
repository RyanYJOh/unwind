# PRD 개정 기록

원문: `unwind-prd-v1.md` / `GHOST_RIVE_BRIEF.md`. 모든 개정은 발주자 컨펌을 받았다.

## 2026-08-06
| 조항 | 원문 | 개정 |
|---|---|---|
| §8.1 S3 | bg #A8735A 등 | 대비 4.5:1 불가 → bg #8D5C44 계열로 어둡게, 텍스트 전환점 0.62→0.67 |
| 브리프 §4 | 흰자 분홍 #FFD8D8 | 항상 흰색 (분홍 제거) |
| 브리프 §4 | 충혈선 60% | 제외 (PRD §7.3 '충혈된 눈' 금지 유지). 다크서클은 허용 |

## 2026-08-07 (실기기 검증 후)
| 조항 | 원문 | 개정 |
|---|---|---|
| §6.1 | 항목 탭 = 완료 토글, 등 아이콘 + 흐려진 텍스트 | 항목 = 발광 패널(웜 화이트), 우측 스위치 = 토글, 패널 탭 = 편집, 롱프레스 = 메뉴 |
| §8.4 | (듀오링고식 금지 유지) | 입체감은 빛으로: 발광 + 부드러운 그림자 + 아랫면 두께 — 꺼지면 빛과 함께 사라짐 |
| §9.2 | 0ms lightImpact | "타닥" = lightImpact → 45ms → mediumImpact |
| §6.4 | 임계점 mediumImpact 1회 | + tension 연속 햅틱: 당길수록 틱 간격 150ms→38ms |
| §6.2 | 당기면 펼침, 별도 탭·토글 없음 | 토글 방식 + 상태 DB 영속 (재실행 시 유지) |
| 브리프 §3 | (아웃라인 없음) | Snap 마스코트풍 검은 잉크 아웃라인 (몸통·팔·눈) |
| 브리프 §3 | 파츠에 눈썹 없음 | 눈썹 추가 — 졸림·평온·기쁨만 (§1.3 금지 유지: 찡그림·화남 없음) |
| 브리프 §4 E | 잠들면 움직임 최소 | + 평온하고 만족스러운 미소 |

## 2026-08-07 (2차)
| 조항 | 원문 | 개정 |
|---|---|---|
| §5.3·§6.4·§14 | 당긴 후 t=1.0 고정, 전등 줄 비활성 | 취침 후 스위치 ON = 깨우기(undo): 취침 기록 삭제, §5.2 규칙으로 조도 재계산, 전등 줄 재활성. 깨울 때 유령은 기지개+미소(wakeUpHappy) |
| §6.1 | (토글 UI 미정) | 스위치 = 진짜 벽 로커 스위치 (플레이트+3D 로커+호박색 인디케이터) |
| §8 | (패널 상세) | 형광등 리얼리즘: 점화 플리커, 면광원 코어, 이중 발광, 어두울수록 발광 증폭 |

## 2026-08-07 (3차 — 디자인 개편, 롤백 가능)
kRoomDesign 상수 한 줄로 이전 형광등 디자인 롤백 가능 (design_variant.dart).
| 항목 | 내용 |
|---|---|
| 광원 | 우측 상단 천장 조명(갓+전구, 잉크 아웃라인) — 방의 유일한 광원. 조도(1-t)에 비례해 감쇠, 전등 줄이 그 아래 |
| 아이템 | 형광등 패널 → 담백한 벽 스위치 행. 끄면 천장 조명이 어두워지고 화면이 라이트→다크 |
| 도미노 | 순차 소등 시 각 항목 마이크로 바운스(240ms, 0.945→1.03→1) |
| 기타 | ITSAppUsesNonExemptEncryption=false (수출 규정 질문 제거) |

## 2026-08-07 (4차 — darkGlow, 최종 컨셉)
kRoomDesign = darkGlow 기본. ceilingLight(조명 그림)·fluorescent(패널)로 롤백 가능.
| 항목 | 내용 |
|---|---|
| 베이스 | 앱은 항상 다크 (§8.1 라이트→다크 램프 이동을 대체 — UI 팔레트는 S5 고정) |
| 빛 | 우측 상단 코너에서 순수한 빛(조명 그림 없음). 4겹 RadialGradient — 워시/미드/코어/핫스팟 |
| 감쇠 | 체크할수록 빛의 반경·강도가 잦아들고, 전부 체크하면 완전한 다크 |

## 2026-08-08 (홈 개편 + 캐릭터 전면 재디자인 + 생활 모드)
| 항목 | 내용 |
|---|---|
| 레이아웃 | 주간 스트립 → 화면 최하단, FAB 확대(68)+주황, 설정 아이콘 좌상단(주간 뷰의 Settings 제거), 추가 시 상단 푸시형 토스트, 신규 작성 시트에도 Add 버튼 |
| 밝은 화면 | 주간 뷰·설정에 SystemUiOverlayStyle.dark (상태바 가독성) |
| 캐릭터 | 카와이 레퍼런스 매칭: 솔리드 타원 눈(흰자·동공·눈썹 제거), 혀 보이는 열린 미소, 볼터치, 그라데이션 셰이딩, 잉크 아웃라인 |
| 생활 모드 | ToddMode {day, nightAwake, asleep}. 낮(06~19) 2시간 슬롯 일과 7종. 밤+미완 = dazzle(1-t) 기반 눈부심/꾸벅. 전부 체크(시간 무관)·밤 빈 방 = 취침. 서비스(리마인더 등)는 실오늘 프로바이더 유지 |
| §7.3 | Todd 크기 240→118 (홈), 유령 영역 축소로 체크리스트 확대 |
| §9.2 | 완료 항목 삭선(시각 소등과 구분되는 isDone), 스위치 인디케이터 3겹 발광 |

## 2026-08-09 (날짜 열람 + 하단 스트립 확장 + 캐릭터 디테일)
| 항목 | 내용 |
|---|---|
| §6.2 대체 | 스트립 탭 = 주간 뷰가 아니라 **그 날짜의 방으로 홈 전환** (selectedDate/viewedDayKey 계열 신설, 서비스는 todayKey 계열 유지). 주간 뷰는 진입점 없이 보존 |
| 스트립 | 최근 30일 가로 스크롤(reverse — 오늘이 오른쪽 끝), 정방형 창 + 그날 밤 Todd의 눈(밝음=졸린 실눈/소등=감은 눈+완주 시 스마일), 라벨 "월.일" |
| Bill | 하단 우측 이미지 버튼(assets/images/bill.png, 래퍼 없음). 현재 더미 데이터 |
| 과거 열람 | 조도=finalT 스냅샷, Todd=그날 밤 모습, 전등 줄 비활성, FAB 추가는 그 날짜로, 타이틀=날짜 |
| 캐릭터 | 반원 돔(arcToPoint)+매끄러운 옆선, 작은 미튼 손, 얇은 아웃라인(4.2u), 손에 쥔 소품(잔·쿠키), 독서 안경+큰 책, 산책 기울기 8° |
| 캐릭터 레퍼런스 재설계 | 사용자 제공 이미지 기준으로 좁은 반원 머리에서 통통한 하단까지 폭이 계속 넓어지는 실루엣, 짧은 플리퍼 팔, 흰→라벤더 그라데이션과 좌우 하단 음영을 적용. 졸림 표현 가독성을 위해 눈은 레퍼런스보다 약 15% 강조 |
| Todd 눈 디테일 | 솔리드 잉크 타원에서 흰자·네이비 홍채 그라데이션·동공·이중 캐치라이트·윗눈꺼풀 선이 있는 세미리얼 눈으로 변경. 기존 졸림·깜빡임·취침 눈꺼풀 전환은 유지 |
| Todd 상태 디테일 | 커피·간식은 오른쪽 기본 팔을 숨기고 소품으로 이어지는 팔 하나만 표시, 독서는 바깥 팔을 모두 숨기고 책을 받치는 전용 양팔 표시. 커피잔·책 가독성 확대. 눈부심과 꾸벅은 위·아래 곡면 눈꺼풀과 주름으로 세미리얼하게 표현 |
| Todd 상태 단순화 | 소품용 내부 팔은 곡선 경로 대신 회전된 캡슐 기본 도형으로 단순화. 강한 눈부심은 눈꺼풀 압착, 안쪽이 들린 눈썹, 눈꼬리 주름을 추가해 괴로운 표정을 강화 |
| Todd 소품 손 | 커피·책·간식 상태의 내부 팔 표현을 완전히 제거하고, 소품에 겹친 단순한 손 도형만 표시 |
| §1.3 완화 | 밤 눈부심에 한해 찡그린 ∩눈 허용. 밤 기본 입은 처진 곡선(웃음 금지, asleep 제외), 하품은 동그란 O |
| Todo 입력·편집 | 키보드 위 바를 좌 달력 아이콘/중앙 날짜 이동(큰 chevron)/우 원형 화살표 저장 CTA로 통일. 저장 후 키보드 닫힘. 주간·월간 반복 라벨은 선택 날짜를 반영한 `Every Monday`/`Every 3rd`. 편집 날짜 변경 시 해당 날짜로 실제 이동 |
| Todo 삭제 | 롱프레스 메뉴는 Delete만 제공. 반복 항목은 단일 인스턴스/선택 날짜 이후 반복 전체 삭제 중 선택. 모든 항목은 왼쪽 스와이프로 단일 삭제 가능. 반복 단건 삭제는 숨김 tombstone을 남겨 재전개 시 복원되지 않게 함 |
| Todo 자동 미루기 | 비반복 항목에만 선택 가능. 롤오버 시 과거 방의 finalT를 먼저 봉인하고, 모든 과거 날짜의 미완료 원본 행을 오늘로 이동해 과거 화면에서는 사라지게 함. 반복과 자동 미루기는 상호 배제 |
| Todo 선택 시간 | 0~1439분의 선택 시간 저장. 시간 있는 항목을 이른 순으로 먼저 표시하고, 시간 없는 항목은 기존 sortIndex 순서를 유지. 반복 규칙의 시간은 모든 생성 회차에 전파 |
| Todo 시간 알림 | 지정 시각 10분 전에 제목을 포함한 로컬 알림. 논리 날짜의 dayStartHour 이전 시각은 다음 캘린더 날짜로 해석. 실제 기기 타임존을 사용하며 완료·삭제·시간 제거·오늘 소등 시 예약 취소 |

## 2026-08-12 (디자인 시스템 v2 — 듀오링고 문법, 발주자 컨펌)
**§8.1 조도 램프 폐기.** 색은 더 이상 t에서 파생되지 않는다. 앱은 항상 다크이고,
t는 "빛의 양"만 몬다 (CornerGlow 세기 · 창문 셀 밝기). 컨셉(§1 "할 일 하나 = 등
하나", "남긴 불빛만큼 전기요금")은 그대로다.

| 조항 | 원문 | 개정 |
|---|---|---|
| §8.1 | S0 정오~S5 밤 6정거장 램프 + OKLab 보간, t마다 bg/surface/text가 바뀜 | **고정 다크 팔레트** `UnwindColors` (core/tokens/palette.dart). OKLab 보간은 `UnwindColors.window(light)`(주간 스트립 창)에만 남는다 |
| §8.1 | textPrimary 크로스페이드(kTextFlipT 0.67, 180ms) | 폐기 — 배경이 항상 어두워 밝은 글자 하나뿐. `PrimaryText` 위젯 삭제 |
| §8.1 | 액센트는 lamp 하나 | 포인트 컬러 2개: **accent 앰버 #FFB224**(빛·CTA·선택), **danger 코랄 #FF6B5A**(삭제·경고 전용). 그 외 전부 슬레이트-네이비 중립 |
| §8.4 | 입체감은 빛으로 (발광 + 그림자) | **듀오링고식 3D**: blur 0의 solid offset shadow 4px. 누르면 그 위로 내려앉는다. §11 "블러 금지"와 충돌하지 않는다 |
| §8.2 | display w600 / title w600 / body w400 | 전반적으로 무겁게 — display·title w800, label·button w700~800. `FontVariation('wght')`로 가변 폰트 인스턴스를 직접 지정 |
| §8.3 | radius sm8/md12/lg20 | xs8/sm12/md16/lg24/xl32 (두툼하게). `UnwindDepth`(압출 깊이)·`UnwindStroke`(테두리 2px) 토큰 신설 |
| §6.1 | 아이템 = 발광 패널/형광등 (점화 플리커·이중 발광) | **두툼한 타일 + 납작한 벽 로커 스위치.** 켜진 등은 앰버 테두리로만 구분 — 빛의 총량은 CornerGlow가 독점한다 |
| §6.1 | 롱프레스 → 삭제 → (반복이면) 범위 선택 | 반복 항목은 롱프레스에서 **곧바로** 범위 선택 (중간 단계 제거) |
| §6.3 | 자동 미루기 = 체크박스, 반복 = 칩(‘반복 없음’ 포함) | 옵션은 전부 칩. 선택된 칩을 다시 누르면 해제 (‘반복 없음’ 칩 제거) |
| §6.2·§6.7 | 주간 뷰·설정은 밝은 중립 테마 고정 | 라이트 모드 폐기 — 앱 전체가 다크. **예외: 주간 청구서 영수증**은 공유 이미지라 밝은 종이 유지 (§6.5) |
| §9.2 | 화면이 햅틱을 직접 호출 | 컴포넌트가 담당 — `UnwindPressable`이 모든 탭에 햅틱을 붙이고, `UnwindHapticsScope`가 설정을 트리에 흘린다 |
| 롤백 | kRoomDesign(ceilingLight·fluorescent) 보존 | 램프 팔레트에 묶여 있어 제거. 되살리려면 git 히스토리 참조 |

## 2026-08-12 (디자인 시스템 v2 — 2차 조정, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §8 조명 | CornerGlow를 절제 (팔레트를 덮지 않게) | **눈부실 만큼 밝게.** 할 일이 가득한 방은 Todd가 못 잘 정도여야 하고, 그래야 한 칸씩 끌 때 어두워지는 게 읽힌다. 빛 응답은 easeOut → **선형**(위쪽 구간이 평평해 차이가 안 보였다) |
| §9.2 | 체크 햅틱 "타닥" = light → 45ms → medium | 등을 **끄는** 쪽은 `switchOff()` = **medium → 55ms → heavy**. 이 앱에서 가장 센 촉감이다. 다시 켜는 쪽만 기존 "타닥" 유지 |
| §6.1 | 스와이프 삭제는 즉시 삭제 | 롱프레스와 **같은 경로**를 탄다 — 반복 항목이면 스와이프에서도 삭제 범위를 묻고, 취소하면 항목이 제자리로 돌아온다 (버그 수정) |
| §6.1 | 삭제는 되돌릴 수 없음 | 삭제 후 **되돌리기가 달린 상단 토스트**(5초). `TodoRepository`가 `TodoUndo` 콜백을 돌려주고 peak까지 삭제 직전 값으로 복원한다. 반복 전체 삭제도 스냅샷을 떠 두어 되돌릴 수 있다 |
| §6.3 | 추가 확인 토스트 | 제거 (시트가 닫히고 등이 늘어나는 것이 피드백). 토스트는 삭제 되돌리기 전용으로 남는다 |

## 2026-08-12 (디테일 UX — 3차, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §7.3 | Todd는 외부 이벤트(checkOff/allDone/wakeUpHappy)에만 반응 | **탭하면 반응한다**(`poke`). 반응은 렌더러가 모드를 보고 고른다 — 깨어 있으면 **간지럼**(몸 떨림 + ∩ ∩ 웃는 눈 + 활짝 벌어진 입, 1.3s), 졸리면 **실눈 두리번**(한쪽 눈만 겨우 뜨고 좌→우→정면, 2.2s), **잠들었으면 무반응**(햅틱조차 없음 — 깨우지 않는다) |
| §6.4 | 놓으면 스프링으로 원위치 복귀 | **진짜 줄처럼 논다.** 세로는 원위치를 지나쳐 튀었다가 통통 잦아들고(ζ≈0.34, 위로 20px 제한), 가로는 느린 진자로 흔들린다(주기 ≈1s). 줄 중간은 구슬 변위의 60%. 이전 구현은 오버슈트를 잘라내 튕김이 보이지 않았다 |

## 2026-08-13 (주간 뷰 재구축, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.2 | 주간 뷰는 오버레이 토글, 상태를 설정에 영속(`weekViewOpen`) | **라우트**로 전환. 진입점은 홈 제목 옆 `Week n` 알약(ISO 8601 주차). 죽어 있던 `weekViewOpen` 설정 제거 |
| §6.2 | 주간 뷰에서 체크 가능 | **체크 불가.** 등을 끄는 행위는 오늘의 방에만 남긴다 — 주간 뷰의 등은 읽기 전용 표시(`UnwindTodoTile.readOnlySwitch`). 추가·편집·삭제는 홈과 동일 |
| §1 | 진행률·퍼센트·개수 등 생산성 어휘 금지 | 주간 뷰 최상단에 **빛 막대**를 둔다. 진행률 바가 아니라 "이번 주에 아직 켜져 있는 등"의 양이며, 끌수록 앰버가 줄어든다. 숫자·퍼센트·체크마크는 여전히 쓰지 않는다 (§1 유지) |
| §5.5 | 칩은 상호배타 선택 전용 | 이동·알림용 **`UnwindPill`** 신설. 칩(선택)과 알약(이동)을 컴포넌트로 분리해 역할이 섞이지 않게 한다. 청구서 배지도 이걸로 통일 |

## 2026-08-13 (주간 뷰 2차 조정, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §1 | 진행률·퍼센트·개수·체크마크 같은 **생산성 어휘 전면 금지** | **완화.** 생산성 어휘를 쓸 수 있다. 단 홈(오늘의 방)은 여전히 은유로만 말한다 — 방의 빛에 숫자를 얹지 않는다. 계획을 훑는 자리(주간 뷰)에서만 진행 표시를 허용 |
| §6.2 | 주간 뷰 상단은 "남은 빛" 막대 (끌수록 줄어듦) | **진행 바** — 끝낸 만큼 차오른다. §1 완화의 첫 적용 사례 |
| §6.2 | 요일 우측에 `+` 하나 | `+`(추가) + `›`(**그 날짜의 방으로 이동**) 두 개. 홈의 열람 날짜를 바꾸고 주간 뷰를 닫는다 |
| §6.2 | 읽기 전용 타일은 끌 수 없는 앰버 등 표시 | **표시 제거.** 완료 여부는 테두리 색으로만 구분한다 — 앰버를 남기면 "누르면 체크된다"로 오인된다 |
| §5.5 | `UnwindPill` 높이 32pt, chevron 옵션, 압출 없음 | 26pt로 축소 + 듀오링고식 압출 3pt + chevron 제거. 제목 옆 보조 요소라 작아야 한다 |

## 2026-08-13 (홈 하단·상단 재배치, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.1 | 좌상단 설정 아이콘 / 하단 우측 Bill 버튼 | **Bill이 좌상단으로**, 설정은 제목 오른쪽에 작게(32pt). 미확인 배지는 Bill 옆에 **코랄** 알약 — 앰버는 앱 전체가 쓰는 색이라 알림으로 읽히지 않는다 |
| §6.2 | 하단 스트립 = 최근 30일 가로 스크롤, 라벨 "8.2" | **주 단위 페이징**(한 페이지 = 월~일). 너비를 다 쓰고, 라벨은 **요일**(Mon·Tue…). 범위는 이번 주 ±52주 — 미래 1년 상한은 발주자 결정. 아직 오지 않은 날은 얼굴을 그리지 않는다 |
| §6.2 | 주 진입 칩은 홈 제목 옆, "Week N" | **좌하단**(스트립 바로 위)으로 이동. 라벨은 "이번 주 / 지난주 / 다음주 / Jul 27 – Aug 2". 스트립을 넘기면 칩이 따라가고, 누르면 **그 주의** 주간 뷰가 열린다 |
| §6.2 | 주간 뷰는 이번 주만, 제목은 ISO "Week 33" | `WeekScreen(mondayKey:)`로 **아무 주나** 연다. 제목은 칩과 같은 라벨(`week_label.dart`)을 공유 — 이름은 한 곳에서만 만든다 |
| §6.2 | 요일 우측에 `+`와 `›`가 나란히 | `›`는 **날짜 바로 옆**, `+`는 우측 끝. 붙여 두면 무엇이 무엇인지 헷갈린다 |

## 2026-08-13 (홈 하단 정렬 · 입력 시트 높이, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.1 | FAB가 리스트 위에 떠 있음 | 주 칩과 **같은 줄**로 내린다. 떠 있으면 칩이 그만큼 밀려 올라가고 둘의 높이가 어긋난다 |
| §5.5 | 중립 알약 압출면 = `solid` | `pillDeep`(#101B26) 신설 + 깊이 3→4pt. `solid`(#060B12)는 배경 `ink`(#0D1520)와 명도 차가 거의 없어 그림자가 보이지 않았다 |
| §6.3 | 반복 칩은 Wrap(여러 줄) | **가로 스크롤 한 줄.** 여러 줄로 접히면 키보드까지 올라왔을 때 시트가 화면을 넘긴다 |
| §6.3 | 구획 여백 s20 | s12로 축소 + 전반적으로 한 단씩 축소. 시트 ≈417pt + 키보드 ≈336pt = 753pt (iPhone 17 874pt) |

## 2026-08-13 (조명 3차 · 하단 정렬, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §8 조명 | CornerGlow 2차 값 | **3차 증폭.** 할 일이 하나라도 남으면 눈이 부셔 잠들 수 없는 밝기. 상한은 §12가 정한다 — 이 값에서 헤더 제목이 4.5:1을 겨우 넘는다 |
| §12 | 홈 헤더 설정 아이콘 = `textSecondary` | `textPrimary`로 한 단 올린다. 밝아진 글로우 위에서 2.2:1까지 떨어져 UI 요소 3:1을 깼다 |
| §6.1 | 주 칩과 FAB가 세로 가운데 정렬 | **하단 라인을 맞춘다.** 가운데 정렬하면 작은 칩이 위로 떠서 스트립과 멀어진다 |

## 2026-08-15 (세계관 정착: 취침·기상시간과 다크서클, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §4.5·§5.3 | 하루 경계 = `dayStartHour`(기본 06시), Todd와 무관한 설정 | **Todd 기상시간(기본 05시)으로 통합.** "Todd가 일어나는 순간 새 하루가 시작된다" — 롤오버·반복 전개·청구서 생성이 전부 기상시간에 일어난다. 설정 키는 `wakeHour`(구 `dayStartHour` 값은 읽기 폴백으로 존중) |
| §7.3 | 밤의 시작 = 상수 19시 (`kToddNightHour`) | **Todd 취침시간(기본 22시) 설정** 신설. 낮(일과)은 기상시간~취침시간, 그 후 불이 남아 있으면 못 자는 밤. 자정 넘는 취침시간(새벽 1~2시)도 허용. 온보딩에서 받을 예정(현재는 설정에서 변경) |
| §10 | 밤 리마인더 시각은 별도 설정 (`nightReminderTime`, 기본 22:00) | **취침시간과 통합.** "취침 알림"으로 개명 — Todd의 취침시간 정각에, 미완 항목이 있고 소등 전일 때만 발송. 별도 시각 설정 제거 |
| §7.2 | 다크서클은 졸림(sleepiness)에만 비례하는 표정 요소 | **전날 불을 남긴 밤의 흔적** 신설. 롤오버 봉인 시 미완 항목이 남았고 소등도 안 했으면 `days.restless`(신규 컬럼, v3 마이그레이션)로 기록하고, 다음날 Todd는 평소처럼 행동하되 눈 밑에 옅은 다크서클이 하루 종일 남는다(감은 눈·웃는 눈 밑에도). 빈 방이었거나 다 껐거나 줄을 당긴 밤은 해당 없음. 과거 날짜 열람 시에도 그날 기준으로 적용 |
| 시간 단위 | — | 취침·기상시간은 **정시 단위**(발주자 결정). 분 단위가 필요해지면 날짜 유틸 전반을 확장해 대응 |

## 2026-08-15 (Todd 디테일 2차, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §7.3 소품 | 커피·간식·독서 소품에 쥔 손을 겹쳐 그림 (2026-08-09) | **손 제거.** 몸통 안에 손이 있는 것처럼 보였다 — 소품(잔·책·쿠키)은 몸 앞에 그냥 둥둥 떠 있다. 안경은 유지 |
| §7.2 다크서클 | 옅은 반달 선 하나 (카와이 톤 절제) | **과장.** 홈의 캐릭터가 작아(118pt) 절제하면 안 보인다 — 채운 초승달 음영 + 굵은 가장자리 선(세기 비례로 폭·굵기 증가), restless 바닥값 0.52 → 0.95 |

## 2026-08-15 (디테일 3차, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.1 | 과거·미래 날짜 열람 시에도 Todd를 그림 (과거는 그날 밤 스냅샷) | **Todd는 오직 오늘의 방에만.** 과거·미래 열람은 빈 자리 — 바닥 그림자 타원(블러 0 솔리드) + "Todd는 오늘의 방에 있어요" 한 줄. 탭 무반응 |
| §6.4 | 전등 줄은 불만 끄고 항목 상태는 pending 유지 (§15 미결정) | **일괄 소등 = 일괄 완료.** 남은 pending을 전부 done으로 체크 (completedAt = 당긴 시각 — 청구서 등 사용 시간 계산과 일치). 미결정이던 §15 처리를 확정 |
| §6.3 | 메모는 "Add a note" 라벨 탭 → 힌트 "Note"인 입력란 펼침 | **항상 열린 입력란 하나** (힌트 "Add a note"). 탭 즉시 입력 가능, 이중 placeholder 제거. 제목과의 간격 s4 → s8 |

## 2026-08-15 (디테일 4차, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.1 | Todd 빈 자리 = 그림자 타원 + "Todd는 오늘의 방에 있어요" 한 줄 | **문구 제거** — 그림자만으로 부재가 읽힌다. 스크린 리더용 Semantics 라벨만 유지 |
| §5.5 | UnwindPill은 chevron 없음 (2026-08-13에 제거) | 이동용 알약에 한해 **작은 `›` 재도입**(`chevron: true`) — 주 칩이 눌러볼 수 있는 것임을 알린다. 알림용(청구서 배지)은 그대로 없음 |
| §6.3 | 입력 필드 키보드 기본 소문자 | 할 일·메모 입력은 **문장 첫 글자 자동 대문자**(`TextCapitalization.sentences`, `UnwindTextField` 옵션 신설) |

## 2026-08-15 (iOS 홈 위젯 신설, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §1 어휘 | 숫자 허용 범위는 "계획을 훑는 자리(주간 뷰)"뿐 — 홈(오늘의 방)은 은유로만 | **iOS 홈 위젯을 '흘끗 보는 자리'로 신설, 숫자 허용.** 위젯은 오늘의 방이 아니라 잠금 해제 직후 훑는 표면이므로 남은 개수를 숫자로 명시한다. 앱 안 홈의 은유-온리 정책은 그대로 |
| 위젯 사양 | — | **소형(2×2) 한 종.** Todd는 앱 페인터로 사전 렌더한 스프라이트 PNG(모드×다크서클 조합)를 번들하고, WidgetKit 타임라인이 기상·일과 슬롯·취침 전환을 앱 없이 수행한다. 기상시간 이후 앱을 열기 전까지는 **개수를 숨기고 아침 상태만** 표시 — 롤오버(자동 미루기·반복 전개)가 앱에서만 실행되므로 확정 전 숫자를 보여주지 않는다 |

## 2026-08-15 (온보딩 전면 개편, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.6 | 3화면 온보딩 (컨셉 → M0 샘플 방 체험 → 입력 시트 자동 오픈) | **11페이지 플로우**: 환영(1초 잠금) → 눈부신 밤(1초 잠금) → 소등 체험(더미 3개 전부 꺼야 통과) → 청구서(탭하면 영수증 인쇄 연출) → 질문(매일 항목·취침/기상 시각·이름) → 원형 타임테이블 → 인사. 완료 후 입력 시트 자동 오픈은 제거 — 질문 1이 이미 방에 등을 켜 둔다 |
| §6.6 질문 1 | — | 매일 하는 일 입력(0개면 "아직 없어요" 스킵) → **매일 반복 규칙**으로 등록. 없으면 디폴트 "물 2L 이상 마시기" 생성 (발주자 결정) |
| §6.6 질문 2·3 | — | 유저 취침시각 -3h = Todd 취침시간, 유저 기상시각 -1h = Todd 기상시간. 계산값은 클램프 없이 그대로 저장 (발주자 결정) — 설정 피커 범위를 기상 1~12시·취침 16~02시로 확장 |
| §4.5 | — | `userName` 설정 신설 — 온보딩 마지막에 선택 입력, 인사에 사용. 홈 활용은 추후 결정 |
| iOS | 최소 iOS 13.0 | **15.0** — home_widget·App Store Connect 요구 (2026-08-23, 14.0→15.0) |

## 2026-08-15 (온보딩 2차 조정, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.6 | 11페이지 (밤 안내 → 소등 체험 분리) | **10페이지** — 밤 안내 페이지를 소등 체험과 병합, 유저가 바로 스위치를 만진다. 타일은 홈과 같은 폭(fullBleedContent) |
| §6.6 | hero Todd 118~150 | **200~210으로 확대** — 첫 페이지 수준. 제목 위에도 s20 여유(진행 바에 붙지 않게) |
| §6.6 질문 1 | `+`로 추가해야 "다음" 활성 | **입력 시작 즉시 "다음" 활성** — "다음"이 쓰다 만 입력도 저장하고 진행. `+`는 여러 개 적을 때 줄 추가 용도 |
| §6.6 질문 2·3 | 피커 190pt, 햅틱 없음 | 224pt로 확대 + 굴러갈 때 selection 햅틱 |
| §6.6 이름 | 건너뛰기 가능(선택) | **건너뛰기 제거** — 이름은 필수 |

## 2026-08-15 (체크 축하 리액션, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §7.3 checkOff | 통통 — 0.5s 감쇠 스케일 펄스뿐 (표정 변화 없음) | **체크 축하** 0.8s — 통통 스케일(증폭) + 폴짝 점프 + ∩∩ 웃는 눈·활짝 미소·볼터치(간지럼과 같은 기쁨 문법) + 정수리 부채꼴 별 버스트(춤의 다이아몬드 스파클 재사용). 등을 끌 때마다 짜릿한 심리적 보상을 준다. Reduce Motion 시 점프·별·표정 생략 |

## 2026-08-16 (캐릭터 이름 Lumi → Todd, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| 세계관 | 유령 이름은 **Lumi** (한국어 스크린리더 라벨 "루미") | **Todd** (한국어 "토드"). 카피·알림·온보딩·설정·위젯 표시명·코드 식별자·파일명까지 전부 개명. iOS 위젯 타깃/kind/`com.unwindapp.unwind.ToddWidget`도 함께 변경 — 기존 홈 위젯은 다시 올려야 한다 |

## 2026-08-16 (홈 화면 앱 이름, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| 홈 화면 이름 | `CFBundleDisplayName` = Unwind (언어 不分) | **기기 언어가 한국어면 `토드`, 그 외 `Todd`.** `InfoPlist.strings`(en/ko)로 로컬라이즈. 앱 안 언어 설정이 아니라 기기 언어를 따른다 (iOS 홈 화면 규칙) |

## 2026-08-16 (취침 알림 30분 전, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §10 | 취침 알림은 Todd 취침시간 **정각** (미완+미소등일 때만) | **30분 전.** 기본 22:00이면 21:30에 발송. 별도 리마인더 시각 설정은 그대로 없음 |

## 2026-08-16 (알림 권한 타이밍, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §10 | 알림 권한은 온보딩 종료 후(인사 2.1초 → 플래그 → 권한 → 홈) | **인사 화면 도착 0.5초 뒤에 요청.** 미리보기(dev)는 묻지 않음. 홈으로 넘어갈 때 다이얼로그가 남아 있으면 답을 기다린다 |

## 2026-08-16 (아침 인사 + 푸시 설정, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §10 | 알림 3종(취침·청구서·시간 Todo). 설정에 취침·청구서 토글만 | **4종.** 아침 인사 신설(Todd 기상 +1h, 매일 반복, 이름 있으면 부름). 설정 > **푸시** 화면에서 아침 인사 / 하루 마무리 / 시간 알림 / 청구서 도착을 각각 on/off |

## 2026-08-16 (온보딩 위젯 안내, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.6 | 10페이지. 인사 2.1초 뒤 플래그 → 홈 | **11페이지.** 인사 뒤에 위젯 설치 안내("Oh, one last thing.."). 홈 화면 프리뷰 + 설치 3단계. CTA **Got it!** 이 플래그를 세우고 오늘의 방으로 |

## 2026-08-16 (주간 청구서 실데이터, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.5 | 등 하나 = 0.06kWh/h × 점등 시간, 152원/kWh + 기본료 730원. 홈 버튼은 더미 지난주 | **실데이터.** 영수증 세 구역: 완수 `n / m` · 전기요금(요일별 합산) · Todd 상태(이미지+색). 요금은 밤에 남긴 등만 — 취침~기상 × 0.06kWh × 152원, 기본료 없음. 하루를 닫으면 그날 0원, **7일 모두 닫으면 주간 0원.** 수면 등급은 소등한 밤/7 (100% perfect / 80~100% fine / 50~80% tossed / 20~50% barely / 20% 미만 awake) |
| §6.5 열람 | (아무 때나 지난주·더미) | **월요일에만** 지난주 월~일을 연다. 다른 요일은 같은 영수증에 안내. 미확인 점은 월요일에만 |
| §6.5 통화 | 한화 고정 (152원/kWh + 기본료) | **기기 지역 통화.** kWh는 공통, 단가는 대략치 (KRW 152 · USD 0.16 · EUR 0.28 …). 정확할 필요 없음 |

## 2026-08-17 (청구서 단가 100배, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.5 단가 | KRW 152원/kWh 등 실세 근처 | **재미용 100배.** KRW 15,200원/kWh, USD $16/kWh, EUR €28/kWh. kWh·소등 0원 규칙은 그대로 |

## 2026-08-22 (온보딩 첫인사 — 잠꾸러기 토드 깨우기, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.6 첫 페이지 | 환영 — 낮의 방(glow 0.55), Todd가 스스로 까르르, CTA 1초 뒤 활성 | **조명 없는 밤**(glow 0). Todd가 "Hi, I'm To..d.." 인사를 타이핑하다 말끝을 흐리며 **잠들어 버린다**. 상단 토스트가 "톡톡 두드려 깨워 보라"고 안내. **세 번 두드려야** 깬다 — 1·2번째는 실눈 두리번만 하고 도로 잠들고, 3번째에 기지개를 켜며 깨어나 새벽빛(glow 0.25)과 함께 "Oh hey! I'm Todd" + "잠이 많은 편" 본문. CTA는 깨워야만 활성 |
| §6.6 대사 | 전 페이지 정적 텍스트 | **1·2페이지 대사는 타이프라이터**(`UnwindTypewriterText`)로 타이핑 — `.`에서 8배 쉬어 졸린 리듬. 청구서("Every Monday, your bill arrives")부터는 기존대로 정적 |

## 2026-08-22 (주간 스트립 — 앰버 창은 오늘 하나뿐, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.2 스트립 | 지난 날 창 밝기 = 그날 finalT (불을 남긴 날은 앰버로 밝은 창 + 졸린 실눈) | **앰버로 밝은 창은 오늘 하나뿐.** 지난 날은 결과와 무관하게 캄캄한 창 + 감긴 눈. 불(미완)을 남긴 밤(`days.restless`)은 눈 밑 **진한 다크서클**로, 다 끄고 잔 밤은 스마일로 구분한다 |

## 2026-08-22 (방 조명 색 선택, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §8.1/v2 팔레트 | 포인트 컬러는 앰버 `#FFB224` 고정 ("포인트 컬러는 둘뿐. 새 색 추가 금지") | **accent는 조명 색 선택형.** 설정 > 조명 색에서 7색(앰버 기본·노을·로즈·라벤더·하늘·민트·달빛) 중 고르면 CTA·FAB·선택·글로우·창문 램프·iOS 위젯까지 앱 전반의 강조색이 통째로 바뀐다. 역할은 여전히 하나("빛")고 danger 코랄은 고정. 대비 §12는 7색 전부 palette_test가 자동 검증 |

## 2026-08-22 (온보딩 준비 확인 + 별점 팝업, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.6 | 11페이지 — 원형 타임테이블 다음이 곧바로 이름 | **12페이지.** 이름 직전에 **준비 확인** 신설: "나와 함께 하루를 생산적으로 관리할 준비 됐어?" 3지선다(🥱 아니, 별로 / 🙂 어느 정도 / 🤩 응, 기대돼!). "응, 기대돼!"는 하단 프라이머리 자리(여덟 페이지 내내 학습된 진행 버튼 위치)에 두고, 고르면 Todd가 체크 축하로 화답한 뒤 기대가 정점인 순간 **앱스토어 별점 팝업**(in_app_review)을 띄우고 이름으로 넘어간다. 나머지 답은 팝업 없이 진행. 답은 저장하지 않는다 |

## 2026-08-22 (수익화 — Todd Plus, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| 신설 | 수익화 없음 | **Todd Plus** (월간/연간/평생). 원칙: 의식(하루 닫기)은 무료로 완전하게, 관계·표현은 유료로 — 광고는 하지 않는다. 게이트 ① 무료는 반복 규칙 3개까지 (네 번째 저장 시 페이월, 온보딩은 예외) ② 조명 색은 앰버 외 전부 Plus (무료엔 자물쇠, 저장된 색은 유지되고 표시만 앰버로). 페이월은 Todd가 주인공(까르르 → 결제 시 축하), 기능 목록 끝에 "…and many more to come!"으로 확장 기대를 심는다. 가격은 표시용(₩4,900/₩29,000/₩79,000) — StoreKit 연동 시 확정 |

## 2026-08-22 (다크서클 리얼 개정 + 날짜 달력 + Todd 물성, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6 다크서클 | 눈 밑 곡선(+진할 때만 옅은 초승달) | **채운 음영 주머니** (같은 날 2차: 빗금은 과해서 제거 — 음영만). 본체·주간 스트립·위젯 스프라이트(재추출) 모두 적용 |
| §6.3 날짜 선택 | 입력 시트 안 인라인 CupertinoDatePicker 휠 | **월 그리드 달력을 별도 바텀시트로** — 시트가 좁아 인라인은 터치 44pt 불가. 과거 비활성·오늘 링·선택 앰버, 월요일 시작, 오늘~+365일 |
| §6.1 Todd 탭 | 렌더러 표정 반응만 | 온보딩과 같은 **스쿼시&바운스**(ToddPokeSquish 공용화)를 홈에도 — 잠들면 무반응 원칙 유지 |

## 2026-08-23 (설정은 유저 시각, 발주자 컨펌)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.7 기상·취침 | 설정 > Todd의 하루에서 **Todd** 기상·취침을 직접 고른다 | **유저** 기상·취침을 고른다 (온보딩에서 받은 값). 저장 시 Todd 시각은 기존 매핑(기상 −1h, 취침 −3h)으로 파생. 기존 유저(유저 시각 키 없음)는 Todd +1h 로 표시. 캡션에 Todd 시각을 적어 둔다 |

## 2026-08-27 (청구서 요금 절반 + 지난주 비교 + 알림 보완, 발주자 지시)
| 조항 | 원문/직전 | 개정 |
|---|---|---|
| §6.5 단가 | 재미용 100배 (한국 15,200원/kWh, 전 통화 동일 배율) | **절반 인하 — 재미용 50배** (한국 7,600원/kWh). 100배는 유저가 과하다고 느낄 수준이라 전 통화(krw·usd·eur…) 일괄 절반 |
| §6.5 영수증 | 지난주 대비 증감은 Todd 상태 카드의 옅은 캡션 | **총액 바로 아래로 이동 + 강화**: 색(증가 코랄 / 감소 그린)+화살표로 표시. 지난주 청구서가 없으면 그리지 않는다 |
| §6.5 문구 | ko "지난주보다 {n} 많아/적어" (반말) | **경어체 예외**: "지난주보다 {n} 많아요/적어요", "지난주와 같은 요금이에요" |
| §10 ③ | 시간 지정 알림은 권한이 꺼져 있으면 조용히 유실 | 시간을 정해 **저장하는 순간** 권한이 꺼져 있으면 안내 확인 → OS 권한 요청. 거부 고착 상태면 두 번째 시트의 "설정 열기" CTA가 설정 앱의 앱 알림 화면을 연다 (네이티브 채널 `unwind/system_settings`, 토스트는 문구가 잘려 시트로 교체) |
| §10 언어 | (암묵) 예약 시점의 앱 언어 | 한국어 설정인데 영어 푸시가 오던 버그 수정: 설정 로드 전 예약 금지 + NotificationService 예약·취소 직렬화 큐 |
| §6.1 타일 | 메모만 제목 끝 아이콘 | **반복 항목도 제목 끝 반복 아이콘** (hasRepeat — 메모와 같은 문법, 홈·주간 뷰 공통) |
